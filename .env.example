# Crypto Dashboard Environment
# CoinGecko: Public endpoints used for MVP (no key required for basic market data).
# Add keys here only when integrating authenticated or higher-tier APIs.

# Example for future authenticated providers (do not commit real secrets):
# COINGECKO_API_KEY=
# BINANCE_API_KEY=
# BINANCE_API_SECRET=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
